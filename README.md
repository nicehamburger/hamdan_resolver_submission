## Resolver Submission

### To run the tests for index.html:

1. Open PowerShell in this folder.
2. Run: python tests.py.
3. Script will run the tests and return with no errors.

### Dependencies

1. Python 3.x
2. Selenium (pip install selenium)
3. Chrome
4. ChromeDriver
